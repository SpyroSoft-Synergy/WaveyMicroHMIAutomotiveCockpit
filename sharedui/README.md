# SharedUI

Shared UI components used across Wavey applications:

- **WaveyStyle** - Custom Qt Quick Controls style for consistent look and feel
- Common icons and assets
- Reusable QML components (buttons, lists, etc.)

Applications import the style via QML:

```qml
import WaveyStyle
```
